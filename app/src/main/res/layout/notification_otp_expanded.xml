<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="4dp"
    android:paddingEnd="4dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <!-- Header / Sender line -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/notif_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="OTP from 59029411"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/notif_text_primary"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/notif_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="now"
            android:textSize="11sp"
            android:textColor="@color/notif_text_secondary" />
    </LinearLayout>

    <!-- Prominent OTP Row with Quick 1-Tap Copy Action Beside It -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="6dp">

        <TextView
            android:id="@+id/notif_otp_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="5  2  9  7  5  7"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/notif_otp_text"
            android:letterSpacing="0.1"
            android:maxLines="1" />

        <!-- Quick Copy Action beside OTP -->
        <LinearLayout
            android:id="@+id/btn_copy_otp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@drawable/bg_copy_button"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_copy"
                android:contentDescription="Copy OTP" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text="Copy"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/notif_copy_btn_text" />
        </LinearLayout>
    </LinearLayout>

    <!-- Boxy Full Message Container with High Contrast in Light & Dark Mode -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_notification_box"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp">

        <TextView
            android:id="@+id/notif_full_body"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Full message content will display here completely so the user does not need to open the app."
            android:textSize="13.5sp"
            android:lineSpacingExtra="3dp"
            android:textColor="@color/notif_text_primary"
            android:maxLines="30"
            android:ellipsize="end" />
    </LinearLayout>

</LinearLayout>
